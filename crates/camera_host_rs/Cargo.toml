[package]
name = "camera_host_rs"
version = "0.1.0"
edition = "2021"
description = "Host-side WebRTC/webcam stream bridge and zero-copy shared memory buffer pipeline for AndroidWebGPU Camera HAL"

[dependencies]
camera_hal_virtual = { path = "../camera_hal_virtual" }
aidl_compat = { path = "../aidl_compat" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
log = { workspace = true }

[dev-dependencies]
