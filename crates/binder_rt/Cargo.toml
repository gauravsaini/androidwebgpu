[package]
name = "binder_rt"
version = "0.1.0"
edition = "2021"
description = "Pure-Rust AOSP Parcel Codec and Binder Wire Protocol Runtime"

[dependencies]
bytemuck = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true, optional = true }
log = { workspace = true, optional = true }

[features]
default = []
serde = ["dep:serde"]
