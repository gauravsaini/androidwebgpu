[package]
name = "binder_routing"
version = "0.1.0"
edition = "2021"
description = "Selective Binder routing policy engine for AndroidWebGPU"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
log = { workspace = true }

[dev-dependencies]
